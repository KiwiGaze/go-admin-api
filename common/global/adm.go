package global

const (
	// Version is the current version of the application
	Version = "1.0.0"
)

var (
	// Driver is the database driver to use (e.g., "mysql", "postgres", "sqlite3")
	Driver string
)